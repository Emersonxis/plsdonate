local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local VirtualInputManager = game:GetService("VirtualInputManager")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local LocalPlayer = Players.LocalPlayer

-- Remotes
local RollRemote = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("RollPickaxe")

-- Arquivo de Configuração Customizada JSON
local configFileName = "FrancesSTools_Config.json"

-- Anti-AFK
LocalPlayer.Idled:Connect(function()
    VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Space, false, game)
    task.wait(0.1)
    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.Space, false, game)
end)

-- Tabela fixa de Minérios
local OreBaseHealth = {
	["Stone"] = 50,
	["Coal"] = 150,
	["Copper"] = 350,
	["Iron"] = 600,
	["Gold"] = 1000,
	["Diamond"] = 4000,
	["Emerald"] = 6000,
	["Ruby"] = 9000,
	["Topaz"] = 14000,
	["Amethyst"] = 22000,
	["Sapphire"] = 35000,
	["Obsidian"] = 55000,
	["Opal"] = 80000,
	["Crimsonite"] = 110000,
	["Aetherite"] = 150000,
	["Galaxite"] = 200000,
	["Meteor"] = 500000,
	["Mirrorite"] = 10000
}

local PresetOresList = {
	"Todos", "Stone", "Coal", "Copper", "Iron", "Gold", "Diamond", 
	"Emerald", "Ruby", "Topaz", "Amethyst", "Sapphire", 
	"Obsidian", "Opal", "Crimsonite", "Aetherite", "Galaxite", 
	"Meteor", "Mirrorite"
}

-- Variáveis de Controle
local selectedOres = {}
local priorityMode = "Menor Vida"
local autoTpActive = false
local autoRollActive = false

-- Sistema de Voo Estilo Infinite Yield (BodyObjects)
local flyBV, flyBG

local function startInfiniteYieldFly(hrp)
    if not flyBV or not flyBV.Parent then
        flyBV = Instance.new("BodyVelocity")
        flyBV.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        flyBV.Velocity = Vector3.new(0, 0, 0)
        flyBV.Parent = hrp
    end
    if not flyBG or not flyBG.Parent then
        flyBG = Instance.new("BodyGyro")
        flyBG.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
        flyBG.CFrame = hrp.CFrame
        flyBG.Parent = hrp
    end
end

local function stopInfiniteYieldFly()
    if flyBV then flyBV:Destroy() flyBV = nil end
    if flyBG then flyBG:Destroy() flyBG = nil end
end

-- Sistema de Fila Dinâmica
local targetQueue = {}

local function getOreHealth(ore)
    return ore:GetAttribute("Health") or OreBaseHealth[ore.Name] or 0
end

-- Busca o chão de forma dinâmica para NÃO travar o script por StreamingEnabled
local function isOreOnZone9(ore)
    local Zones = workspace:FindFirstChild("Zones")
    local Zone9 = Zones and Zones:FindFirstChild("9")
    local Nature = Zone9 and Zone9:FindFirstChild("Nature")
    local GroundPart = Nature and Nature:FindFirstChild("Ground")
    
    if not GroundPart then return false end -- Se a área 9 não estiver carregada fisicamente no mapa, ignora
    
    local orePosition = ore:GetPivot().Position
    local localPos = GroundPart.CFrame:PointToObjectSpace(orePosition)
    local size = GroundPart.Size
    
    local insideX = math.abs(localPos.X) <= (size.X / 2)
    local insideZ = math.abs(localPos.Z) <= (size.Z / 2)
    local aboveY = localPos.Y >= -(size.Y / 2)
    
    return insideX and insideZ and aboveY
end

local function isValidTarget(ore)
    local isSelected = table.find(selectedOres, "Todos") or table.find(selectedOres, ore.Name)
    if not isSelected or getOreHealth(ore) <= 0 then return false end
    
    local baseHealth = OreBaseHealth[ore.Name] or 0
    if baseHealth >= 4000 then
        return true
    else
        return isOreOnZone9(ore)
    end
end

local function updateQueue()
    for i = #targetQueue, 1, -1 do
        local ore = targetQueue[i]
        if not ore or ore.Parent ~= workspace.Ores or not isValidTarget(ore) then
            table.remove(targetQueue, i)
        end
    end

    if #targetQueue >= 2 then return end

    local validOres = {}
    for _, ore in ipairs(workspace.Ores:GetChildren()) do
        if isValidTarget(ore) and not table.find(targetQueue, ore) then
            table.insert(validOres, ore)
        end
    end

    if priorityMode == "Maior Vida" then
        table.sort(validOres, function(a, b)
            return getOreHealth(a) > getOreHealth(b)
        end)
    else
        table.sort(validOres, function(a, b)
            return getOreHealth(a) < getOreHealth(b)
        end)
    end

    for i = 1, #validOres do
        if #targetQueue >= 2 then break end
        table.insert(targetQueue, validOres[i])
    end
end

-- Loop de Teleporte (Fila Dinâmica + IY Fly fixado)
local lastTrackedTarget = nil
local lastTeleportTime = 0
local initialHealth = 0
local checkedDamage = false

task.spawn(function()
    while task.wait(0.05) do
        local character = LocalPlayer.Character
        local hrp = character and character:FindFirstChild("HumanoidRootPart")
        
        if autoTpActive and #selectedOres > 0 then
            updateQueue()
            local currentTarget = targetQueue[1]
            
            if hrp and currentTarget then
                startInfiniteYieldFly(hrp)
                
                if currentTarget ~= lastTrackedTarget then
                    lastTrackedTarget = currentTarget
                    hrp.CFrame = currentTarget:GetPivot() + Vector3.new(0, 10, 0)
                    if flyBG then flyBG.CFrame = hrp.CFrame end
                    lastTeleportTime = os.clock()
                    initialHealth = getOreHealth(currentTarget)
                    checkedDamage = false
                end
                
                if os.clock() - lastTeleportTime >= 2 and not checkedDamage then
                    local currentHealth = getOreHealth(currentTarget)
                    if currentHealth >= initialHealth then
                        hrp.CFrame = currentTarget:GetPivot() + Vector3.new(0, 10, 0)
                        if flyBG then flyBG.CFrame = hrp.CFrame end
                        lastTeleportTime = os.clock()
                        initialHealth = currentHealth
                    else
                        checkedDamage = true
                    end
                end
                
                local distance = (hrp.Position - currentTarget:GetPivot().Position).Magnitude
                if distance > 25 then
                    hrp.CFrame = currentTarget:GetPivot() + Vector3.new(0, 10, 0)
                    if flyBG then flyBG.CFrame = hrp.CFrame end
                end
            else
                lastTrackedTarget = nil
                stopInfiniteYieldFly()
            end
        else
            lastTrackedTarget = nil
            table.clear(targetQueue)
            stopInfiniteYieldFly()
        end
    end
end)

-- Loop de Auto Roll Ultra Rápido
task.spawn(function()
    while true do
        if autoRollActive then
            task.spawn(function()
                RollRemote:InvokeServer()
            end)
            task.wait(0.01)
        else
            task.wait(0.5)
        end
    end
end)

-- Sistema Customizado de Salvamento JSON para o Delta
local function saveCustomConfig()
    local data = {
        selectedOres = selectedOres,
        priorityMode = priorityMode,
        autoTpActive = autoTpActive,
        autoRollActive = autoRollActive
    }
    local success, str = pcall(function() return HttpService:JSONEncode(data) end)
    if success then
        writefile(configFileName, str)
    end
end

-- Inicialização Limpa da Interface Rayfield
local Window = Rayfield:CreateWindow({
   Name = "FrancesS Tools",
   LoadingTitle = "Carregando Scripts...",
   LoadingSubtitle = "by Frances",
   ConfigurationSaving = { Enabled = false },
   KeySystem = false
})

local FarmTab = Window:CreateTab("Auto Farm", 4483362458)
local RollTab = Window:CreateTab("Auto Roll", 4483362458)

local OreDropdown = FarmTab:CreateDropdown({
   Name = "Selecione os Minérios",
   Options = PresetOresList,
   CurrentOption = {},
   MultipleOptions = true,
   Flag = "Manual_OreSelect",
   Callback = function(Options)
       selectedOres = Options
       table.clear(targetQueue)
       lastTrackedTarget = nil
       saveCustomConfig()
   end,
})

local PriorityDropdown = FarmTab:CreateDropdown({
   Name = "Prioridade de Foco",
   Options = {"Menor Vida", "Maior Vida"},
   CurrentOption = {"Menor Vida"},
   MultipleOptions = false,
   Flag = "Manual_Priority",
   Callback = function(Option)
       priorityMode = Option[1] or "Menor Vida"
       table.clear(targetQueue)
       lastTrackedTarget = nil
       saveCustomConfig()
   end,
})

local TpToggle = FarmTab:CreateToggle({
   Name = "Auto TP Ativo",
   CurrentValue = false,
   Flag = "Manual_Tp",
   Callback = function(Value)
       autoTpActive = Value
       saveCustomConfig()
   end,
})

local RollToggle = RollTab:CreateToggle({
   Name = "Auto Roll Ativo (Spam Rápido)",
   CurrentValue = false,
   Flag = "Manual_Roll",
   Callback = function(Value)
       autoRollActive = Value
       saveCustomConfig()
   end,
})

-- Carregador Estável JSON
local function loadCustomConfig()
    if isfile(configFileName) then
        local content = readfile(configFileName)
        local success, data = pcall(function() return HttpService:JSONDecode(content) end)
        if success and data then
            if data.selectedOres then 
                selectedOres = data.selectedOres 
                OreDropdown:Set(selectedOres)
            end
            if data.priorityMode then 
                priorityMode = data.priorityMode 
                PriorityDropdown:Set({priorityMode})
            end
            if data.autoTpActive ~= nil then 
                autoTpActive = data.autoTpActive 
                TpToggle:Set(autoTpActive)
            end
            if data.autoRollActive ~= nil then 
                autoRollActive = data.autoRollActive 
                RollToggle:Set(autoRollActive)
            end
        end
    end
end

task.spawn(loadCustomConfig)
