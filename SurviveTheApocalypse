--[[
    NAME: Zombies Auto Kill
    VERSION: 5.0.0
    STATUS: Aura Priority & Target Cycling
]]

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local mobFolder = workspace:FindFirstChild("Characters") or workspace:FindFirstChild("Zombies") or workspace:FindFirstChild("NPCs")
local camera = workspace.CurrentCamera

local mobBlacklist = {} 
local healthHistory = {} 
local failCount = {} 
local EXP_CycleKill = false -- Função Experimental 1

-- 1. Visibilidade e Filtro de Distância (300 studs)
local function getValidTargetData(mob, rootPos)
    local head = mob:FindFirstChild("Head") or mob:FindFirstChild("HumanoidRootPart")
    local hum = mob:FindFirstChildOfClass("Humanoid")
    
    if head and hum and hum.Health > 0 and not Players:GetPlayerFromCharacter(mob) then
        local dist = (rootPos - head.Position).Magnitude
        if dist <= 300 then -- Limite de 300 studs
            -- Raycast para verificar obstrução
            local rayParams = RaycastParams.new()
            rayParams.FilterType = Enum.RaycastFilterType.Exclude
            rayParams.FilterDescendantsInstances = {player.Character, camera, mob}
            
            local result = workspace:Raycast(camera.CFrame.Position, (head.Position - camera.CFrame.Position), rayParams)
            
            -- Se não houver nada sólido ou for transparente/sem colisão
            if not result or result.Instance.CanCollide == false or result.Instance.Transparency > 0.7 then
                return {instance = mob, distance = dist, head = head, hum = hum}
            end
        end
    end
    return nil
end

-- 2. Gerenciador de Alvos (Prioridade Estilo Régua/Aura)
local function getSortedTargets()
    local char = player.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not root or not mobFolder then return {} end

    local targets = {}
    for _, mob in ipairs(mobFolder:GetChildren()) do
        if not mobBlacklist[mob] then
            local data = getValidTargetData(mob, root.Position)
            if data then table.insert(targets, data) end
        end
    end

    -- Ordena do mais perto para o mais longe
    table.sort(targets, function(a, b) return a.distance < b.distance end)
    return targets
end

-- 3. Loop Principal
task.spawn(function()
    while true do
        local char = player.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        local tool = char and char:FindFirstChildOfClass("Tool")
        local shootEvent = tool and (tool:FindFirstChild("Shoot") or tool:FindFirstChild("Fire"))

        if char and root and shootEvent then
            local targets = getSortedTargets()
            
            -- Alerta de Proximidade (Panic Mode Automático em 50 studs)
            local isPanic = #targets > 0 and targets[1].distance <= 50
            
            -- Se EXP_CycleKill estiver ON, ele dá apenas 1 tiro por mob e pula pro próximo
            -- Se estiver OFF, ele tenta focar nos 10 mais próximos simultaneamente
            local limit = EXP_CycleKill and #targets or (isPanic and 12 or 10)
            local waitTime = isPanic and 0.05 or 0.1

            for i = 1, math.min(#targets, limit) do
                local data = targets[i]
                local mob = data.instance
                
                -- Anti-Waste Logic
                if healthHistory[mob] and healthHistory[mob] <= data.hum.Health then
                    failCount[mob] = (failCount[mob] or 0) + 1
                    if failCount[mob] >= 5 then mobBlacklist[mob] = true end
                else
                    failCount[mob] = 0
                end
                healthHistory[mob] = data.hum.Health

                -- Disparo na Cabeça
                local args = {
                    [1] = root.Position,
                    [2] = {
                        [1] = {
                            ["Target"] = data.head.Position,
                            ["HitData"] = {{["HitChar"] = mob, ["HitPos"] = data.head.Position, ["HitPart"] = data.head}}
                        }
                    }
                }
                shootEvent:FireServer(unpack(args))
                
                -- Se o modo de ciclo estiver ativo, ele processa um por um rapidamente
                if EXP_CycleKill then task.wait(0.01) end 
            end
            task.wait(waitTime)
        else
            task.wait(0.5)
        end
    end
end)

-- Comandos e Limpeza
player.Chatted:Connect(function(msg) 
    if msg == "1" then 
        EXP_CycleKill = not EXP_CycleKill 
    end 
end)

task.spawn(function() 
    while true do 
        task.wait(4) 
        mobBlacklist = {} 
        failCount = {} 
        healthHistory = {} -- Limpa histórico para evitar cache pesado
    end 
end)
